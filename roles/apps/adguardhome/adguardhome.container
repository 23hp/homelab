[Unit]
Description=AdGuard Home

[Container]
Image=docker.io/adguard/adguardhome:latest
ContainerName=adguardhome

PublishPort=53:53/udp
PublishPort=53:53/tcp

# after proxy_net became internel, add extra network to make it work
Network=proxy_net

Volume=%h/.config/adguardhome:/opt/adguardhome/conf:z
Volume=adguardhome.volume:/opt/adguardhome/work

Annotation=io.containers.autoupdate=registry

Label=service=dns

[Service]
Restart=always
RestartSec=5s

[Install]
WantedBy=default.target
